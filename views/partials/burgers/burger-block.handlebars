<li class="list-group-item">
	{{burger_name}}

	<button class="change-devoured btn btn-danger float-right" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eat Me!{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>
